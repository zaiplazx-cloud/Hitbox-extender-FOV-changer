local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "Hitbox Extender and FOV Changer",
    LoadingTitle = "Hitbox & FOV Tool",
    LoadingSubtitle = "by Voids",
    ConfigurationSaving = {
        Enabled = false
    }
})

-- TABS
local MainTab = Window:CreateTab("Main", 4483362458)
local SettingsTab = Window:CreateTab("Settings", 4483362459)
local CreditsTab = Window:CreateTab("Credits", 4483362460)

-- STATES
local hitboxExtender = false
local fovChanger = false
local wallbangActive = false
local silentAim = false
local espActive = false

-- HITBOX EXTENDER TOGGLE
MainTab:CreateToggle({
    Name = "Hitbox Extender",
    CurrentValue = false,
    Callback = function(Value)
        hitboxExtender = Value

        Rayfield:Notify({  
            Title = "Hitbox Extender",  
            Content = Value and "Enabled" or "Disabled",  
            Duration = 2  
        })
    end
})

-- FOV CHANGER TOGGLE
MainTab:CreateToggle({
    Name = "FOV Changer",
    CurrentValue = false,
    Callback = function(Value)
        fovChanger = Value

        Rayfield:Notify({  
            Title = "FOV Changer",  
            Content = Value and "Enabled" or "Disabled",  
            Duration = 2  
        })
    end
})

-- SILENT AIM TOGGLE
MainTab:CreateToggle({
    Name = "Silent Aim",
    CurrentValue = false,
    Callback = function(Value)
        silentAim = Value

        Rayfield:Notify({  
            Title = "Silent Aim",  
            Content = Value and "Enabled" or "Disabled",  
            Duration = 2  
        })
    end
})

-- WALLBANG TOGGLE
MainTab:CreateToggle({
    Name = "Wallbang",
    CurrentValue = false,
    Callback = function(Value)
        wallbangActive = Value

        Rayfield:Notify({  
            Title = "Wallbang",  
            Content = Value and "Enabled" or "Disabled",  
            Duration = 2  
        })
    end
})

-- ESP TOGGLE
MainTab:CreateToggle({
    Name = "Player ESP",
    CurrentValue = false,
    Callback = function(Value)
        espActive = Value

        Rayfield:Notify({  
            Title = "Player ESP",  
            Content = Value and "Enabled" or "Disabled",  
            Duration = 2  
        })
    end
})

-- BUTTONS (for instant actions)
MainTab:CreateButton({
    Name = "Reset Hitboxes (Instant)",
    Callback = function()
        Rayfield:Notify({
            Title = "Hitbox System",
            Content = "Hitboxes Reset to Default!",
            Duration = 2
        })
        -- Add your hitbox reset logic here
    end
})

MainTab:CreateButton({
    Name = "Set Max FOV (Instant)",
    Callback = function()
        Rayfield:Notify({
            Title = "FOV Changer",
            Content = "FOV Set to Max!",
            Duration = 2
        })
        -- Add your instant FOV change logic here
    end
})

MainTab:CreateButton({
    Name = "Refresh ESP (Instant)",
    Callback = function()
        Rayfield:Notify({
            Title = "ESP System",
            Content = "ESP Refreshed!",
            Duration = 2
        })
        -- Add your ESP refresh logic here
    end
})

MainTab:CreateButton({
    Name = "Optimize Performance (Instant)",
    Callback = function()
        Rayfield:Notify({
            Title = "Optimizer",
            Content = "Performance Optimized!",
            Duration = 2
        })
        -- Add your optimization logic here
    end
})

-- LOOP SYSTEM (for toggles that need constant checking)
task.spawn(function()
    while true do
        task.wait(0.5)

        if hitboxExtender then  
            -- Add your hitbox extender logic here (e.g., resizing Head/HumanoidRootPart)  
            print("Hitbox Extender Active")  
        end  

        if fovChanger then  
            -- Add your FOV changer logic here (e.g., Workspace.CurrentCamera.FieldOfView = ...)  
            print("FOV Changer Active")  
        end  

        if silentAim then  
            -- Add your silent aim logic here  
            print("Silent Aim Active")  
        end  

        if wallbangActive then  
            -- Add your wallbang logic here  
            print("Wallbang Active")  
        end  

        if espActive then  
            -- Add your ESP logic here  
            print("ESP Active")  
        end  
    end
end)

-- SETTINGS
SettingsTab:CreateToggle({
    Name = "UI Blur",
    CurrentValue = false,
    Callback = function(Value)
        if Value then
            -- Add UI blur logic here
            print("UI Blur Enabled")
        else
            -- Remove UI blur logic here
            print("UI Blur Disabled")
        end
    end
})

-- CREDITS
CreditsTab:CreateLabel("Script By Scriptnova")
